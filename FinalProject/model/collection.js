export default class Collection {
    constructor(collection_id, name, view_url, price, image) {
        this.collection_id = collection_id;
        this.name = name;
        this.view_url = view_url;
        this.price = price;
        this.image = image
    }
}
export default class Collection {
    constructor(collection_id, name, view_url, price) {
        this.collection_id = collection_id;
        this.name = name;
        this.view_url = view_url;
        this.price = price;
    }
}
export default class Collection {
    constructor(collection_id, name, price, image) {
        this.collection_id = collection_id;
        this.name = name;
        this.price = price;
        this.image = image
    }
}